{
  "name": "inventaire-dnd-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend minimal pour l'outil d'inventaire spatial D&D (sert la page + une API clé-valeur persistée sur disque).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^7.2.0"
  }
}
